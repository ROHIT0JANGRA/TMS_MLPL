
@model CodeLock.Models.DocketGPRORegister
@using CodeLock.Models;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("CustomerDocketGPRORegister", "Operation", FormMethod.Post, new { @class = "j-forms" }))
{
    <div class="widget-wrap">
        <div class="row">
            <div class="col-sm-4">
                @Html.Label("Select Date", new { @class = "label" })
                <div class="clearfix">
                    <div id="drDate"></div>
                </div>
            </div>
            <div class="col-sm-4">
                <div class="form-group">
                    @Html.LabelFor(model => model.CustomerName, new { @class = "label" })
                    @Html.HiddenFor(model => model.CustomerId, new { @id = "hdnCustomerId" })
                    <div class="input">
                        @Html.Label("lblCustomerName", " ", new { @class = "label-bold", @id = "lblCustomerName" })
@*                        @Html.TextBoxFor(model => model.CustomerName, new { @class = "form-control textlabel bold", @id = "txtCustomerName" })*@
                    </div>
                </div>
            </div>          
        </div>
        <div class="page-separator">OR</div>
        <div class="row">
            <div class="col-sm-4">
                <div class="form-group">
                    @Html.Label(SessionUtility.DocketNomenClature, new { @class = "label" })
                    <div class="input">
                        @Html.TextBoxFor(model => model.DocketNo, new { @class = "form-control", @id = "txtDocketNo" })
                    </div>
                    @Html.ValidationMessageFor(model => model.DocketNo)
                </div>
            </div>
        </div>
        <div class="form-footer">
            <button type="submit" class="btn btn-success primary-btn" id="btnSubmit">View Report</button>
        </div>
    </div>
}

<script>
    var drDate, ddlFromLocationId, hdnCustomerId, txtCustomerCode, lblCustomerName, txtDocketNo, btnSubmit, customerMasterUrl;

    $(document).ready(function () {
        SetPageLoad('Docket', 'MIS Report', '', '', '');
        customerMasterUrl = '@Url.Action("", "Customer", new { Area = "Master" })';
        drDate = InitDateRangeCalYear('drDate', DateRange.LastWeek);
        hdnCustomerId = $('#hdnCustomerId');
        lblCustomerName = $('#lblCustomerName');
        txtDocketNo = $('#txtDocketNo');
        btnSubmit = $('#btnSubmit');
        lblCustomerName.text("@ViewBag.CustomerName");
        //CustomerAutoComplete(txtCustomerCode.Id, hdnCustomerId.Id);
        //txtCustomerCode.blur(function () { IsCustomerCodeExist(txtCustomerCode, hdnCustomerId, lblCustomerName) });
        btnSubmit.click(ViewReport);
    });

    function ViewReport() {
        var prmList = [{ Name: "FromDate", Value: drDate.startDate }, { Name: "ToDate", Value: drDate.endDate }, { Name: "CustomerId", Value: hdnCustomerId.val() == '' ? 0 : hdnCustomerId.val() }, { Name: "DocketNo", Value: txtDocketNo.val() == '' ? 'ALL' : txtDocketNo.val() }];
        var reportInfo = { PrmList: prmList, Name: 'MISREPORT', Description: 'MIS Report' };
        return ShowReport(reportInfo);
    }
</script>